{
  "name": "ai-skills-hub-selfauth-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc -p test/tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "hono": "^4.6.9",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.12",
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.0.0"
  }
}
